import React from 'react';
import { useDrag } from 'react-dnd';

const ColumnTemplate = ({ data }: any) => {
    // const [{ opacity }, drag] = useDrag({
    //     item: data,
    //     collect: (monitor) => ({
    //         opacity: monitor.isDragging() ? 0.4 : 1,
    //     }),
    // });

    return <div>ColumnTemplate</div>;
};

export default ColumnTemplate;
